export class SaveLocationModel {
    constructor(latitude, longitude, place, uuid) {
        this.latitude = latitude;
        this.longitude = longitude;
        this.place = place;
        this.uuid = uuid;
    }
}